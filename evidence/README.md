# evidence/

Captured proofs — one file per proof. Name each `YYYY-MM-DD-<slug>` (e.g. `2026-06-25-vendor-msa.pdf`). Referenced from the `evidence` column in `tracker.md` and from `[in]`/`[done]` lines in `session-log.md`.

This is the "trust but verify" record: when someone asks for confirmation later, it's already here — no hunting.
